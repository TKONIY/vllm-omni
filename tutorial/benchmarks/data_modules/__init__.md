# `data_modules/__init__.py` — 包初始化

## 文件概述

`data_modules/__init__.py` 是一个空的初始化文件，仅用于将 `data_modules` 目录标记为 Python 包，不包含任何代码逻辑。

## 总结

该文件的存在使得 `benchmarks.data_modules` 可以作为 Python 包被导入，子模块（如 `random_multi_modal_dataset`）可以通过标准导入路径访问。
