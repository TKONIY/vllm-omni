# `__init__.py` — NextStep-1.1 模块入口

## 文件概述

该文件是 `nextstep_1_1` 子包的初始化模块，导出 NextStep-1.1 模型的管线和后处理函数。

## 核心类/函数

| 名称 | 类型 | 说明 |
|------|------|------|
| `NextStep11Pipeline` | 类 | NextStep-1.1 图像生成管线 |
| `get_nextstep11_post_process_func` | 函数 | 获取后处理函数 |

## 与其他模块的关系

- 从 `pipeline_nextstep_1_1` 导入核心组件

## 总结

该文件导出了 NextStep-1.1 模型的核心管线和后处理函数。
