# `__init__.py` — OmniGen2 模块入口

## 文件概述

该文件是 `omnigen2` 子包的初始化模块。文件内容为空（仅有一行），表明该模块的导入通过直接引用子模块实现。

## 与其他模块的关系

- 包含 `omnigen2_transformer.py` 和 `pipeline_omnigen2.py` 两个核心文件

## 总结

空的包初始化文件，各组件通过完整路径导入。
