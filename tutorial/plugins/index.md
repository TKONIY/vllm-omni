# plugins 模块索引

本模块提供 vllm-omni 的插件发现和加载机制，基于 Python 的 `entry_points` 系统实现。

## 模块结构

```
plugins/
└── __init__.py    # 插件加载核心逻辑
```

## 文档列表

| 文件 | 说明 |
|------|------|
| [__init__.md](__init__.md) | 插件加载系统 |

## 模块间关系

- 本模块在进程启动时被调用，加载并执行注册的通用插件和平台插件。
- 与 vLLM 的插件系统并行，使用独立的插件组名称。
